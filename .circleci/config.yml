version: 2.1
orbs:
    node: circleci/node@5.0.1
jobs:
    build:
        docker:
            - image: "cimg/base:stable"
        steps:
            - checkout
            - node/install
            - run:
                name: hello
                command: |
                    echo "hello-world"
